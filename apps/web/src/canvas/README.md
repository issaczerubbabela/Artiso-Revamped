# Canvas mount point

This directory is where `packages/renderer`'s two-canvas compositor
(WebGL image layer + Canvas2D grid layer, per
[`docs/architecture/05-canvas-renderer.md`](../../../../docs/architecture/05-canvas-renderer.md))
mounts into the workspace shell. Intentionally empty in Phase 0 — no canvas
or product features exist yet.
